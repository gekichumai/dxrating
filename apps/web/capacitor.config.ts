import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "dev.imgg.gekichumai.dxrating",
  appName: "DXRating",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
