-- Metadata-only generated migration. These indexes require an online build
-- outside Drizzle's transaction, so the reviewed
-- non-transactional manifest owns every DDL operation and postcondition.
SELECT 1;
